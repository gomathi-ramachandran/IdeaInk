/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: true,
  },
   images: {
    domains: ['jgwovfawzkrsfpqxrdse.supabase.co'],
  },
};

export default nextConfig;
